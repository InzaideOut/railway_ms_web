import styles from "./fields.module.css"

export function TextField(params) {
    // TODO: Implement the text field on the design board
    // Hint: Use the styles.textField class
    return (
        <div></div>
    )
}